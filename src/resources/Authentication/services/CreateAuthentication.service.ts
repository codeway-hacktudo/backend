class CreateAuthenticationService {
  async execute() {
    // chamada do serviço de interligencia artificial para validar identidade do usuário
    return true;
  }
}

export default CreateAuthenticationService;
